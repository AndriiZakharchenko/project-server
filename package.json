{
  "name": "6-express-layered-architecture",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start:dev": "nodemon ./src/server.ts",
    "start": "npm run build && ./node_modules/.bin/pm2 start ./dist/src/server.js --name 6-express-layered-architecture",
    "stop": "./node_modules/.bin/pm2 stop 6-express-layered-architecture",
    "build": "mkdir -p dist && cp package.json dist/ && tsc",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "coverage": "jest --coverage --coverageReporters=cobertura --coverageDirectory=.",
    "pretest": "npm start",
    "posttest": "npm stop"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.1",
    "express": "^4.18.2",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.15",
    "@types/uuid": "^9.0.0",
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0",
    "joi": "^17.13.3",
    "nodemon": "^2.0.20",
    "supertest": "^7.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4"
  }
}
