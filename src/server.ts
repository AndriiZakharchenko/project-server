import http from 'http';
// import { .... } from './error-handler';
// import { .... } from './router';

const PORT = process.env.PORT || 8000;

// Implement the server using the http module to implement the API server with tasks what were described in the task description.
